# 📐 Diagramas UML - Sistema SCEE

## 1️⃣ DIAGRAMA DE CASOS DE USO

### Como Montar no Draw.io / Lucidchart:

```
┌─────────────────────────────────────────────────────────────────┐
│                    SISTEMA SCEE - CASOS DE USO                  │
└─────────────────────────────────────────────────────────────────┘

        👤 Cliente                                    👤 Administrador
           │                                                 │
           │                                                 │
           ├──────────( Registrar-se )                      │
           │                                                 │
           ├──────────( Fazer Login )──────────────────────┤
           │                                                 │
           ├──────────( Buscar Produtos )                   │
           │                 │                               │
           │                 │ «extend»                      │
           │                 └──( Filtrar por Categoria )   │
           │                 │                               │
           │                 │ «extend»                      │
           │                 └──( Filtrar por Preço )       │
           │                                                 │
           ├──────────( Visualizar Detalhes do Produto )    │
           │                                                 │
           ├──────────( Adicionar ao Carrinho )             │
           │                                                 │
           ├──────────( Gerenciar Carrinho )                │
           │              │ remover, alterar qtd            │
           │                                                 │
           ├──────────( Finalizar Compra )──────┐           │
           │                 │                   │           │
           │                 │ «include»         │           │
           │                 ↓                   │           │
           │          ( Selecionar Endereço )   │           │
           │                 │                   │           │
           │                 │ «include»         │           │
           │                 ↓                   │           │
           │          ( Calcular Frete )────────┼───────→ [Sistema Correios]
           │                 │                   │
           │                 │ «include»         │
           │                 ↓                   │
           │          ( Processar Pagamento )───┼───────→ [Gateway Pagamento]
           │                 │                   │
           │                 │ «include»         │
           │                 ↓                   │
           │          ( Enviar Confirmação )────┼───────→ [Sistema de Email]
           │                                     │
           ├──────────( Visualizar Meus Pedidos )          │
           │                                                 │
           ├──────────( Gerenciar Endereços )               │
           │                                                 │
           │                                                 ├──( Gerenciar Produtos )
           │                                                 │        │
           │                                                 │        ├──( Criar Produto )
           │                                                 │        ├──( Editar Produto )
           │                                                 │        ├──( Excluir Produto )
           │                                                 │        └──( Upload Imagens )
           │                                                 │
           │                                                 ├──( Gerenciar Categorias )
           │                                                 │
           │                                                 ├──( Visualizar Pedidos )
           │                                                 │        │
           │                                                 │        └──( Filtrar por Status )
           │                                                 │
           │                                                 ├──( Processar Pedido )
           │                                                 │        │
           │                                                 │        ├──( Alterar Status )
           │                                                 │        └──( Cancelar Pedido )
           │                                                 │
           │                                                 └──( Visualizar Clientes )

```

### 📝 Descrição dos Elementos:

**Atores:**
- 👤 **Cliente**: Usuário que compra produtos
- 👤 **Administrador**: Gerente da loja
- 🔧 **Sistema Correios**: Sistema externo para cálculo de frete
- 💳 **Gateway Pagamento**: Sistema externo de pagamento
- 📧 **Sistema de Email**: Sistema externo de notificações

**Relacionamentos:**
- **«include»**: Funcionalidade obrigatória (ex: Finalizar Compra INCLUI Processar Pagamento)
- **«extend»**: Funcionalidade opcional (ex: Buscar Produtos pode ESTENDER com Filtrar)

---

## 2️⃣ DIAGRAMA DE CLASSES

### Como Montar no StarUML / Visual Paradigm:

```
┌─────────────────────────────────────────────────────────────────────┐
│                    CAMADA DE MODELOS - ABSTRAÇÃO                    │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────┐
│     «abstract»          │
│      BaseModel          │◄───────────────┐
├─────────────────────────┤                │
│ # _id: int              │                │ ABSTRAÇÃO
│ # _created_at: datetime │                │ Classe base abstrata
│ # _updated_at: datetime │                │ para todas entidades
├─────────────────────────┤                │
│ + get_id(): int         │                │
│ + validate(): bool      │◄───────────────┘
│ + to_dict(): dict       │
│ {abstract} save(): void │
└─────────────────────────┘
            △
            │ (herança)
            │
    ┌───────┴────────┬──────────────────┐
    │                │                  │
┌───────────────┐ ┌──────────────┐ ┌──────────────┐
│   Produto     │ │  Categoria   │ │   Pedido     │
└───────────────┘ └──────────────┘ └──────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│              CAMADA DE USUÁRIOS - HERANÇA E ENCAPSULAMENTO          │
└─────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────────┐
                    │     «abstract»          │
                    │       Usuario           │
                    ├─────────────────────────┤
                    │ - _id: int              │ ◄─── ENCAPSULAMENTO
                    │ - _nome: str            │      Atributos privados
                    │ - _email: str           │      com underscore
                    │ - _senha_hash: str      │
                    │ - _tipo: str            │
                    ├─────────────────────────┤
                    │ + get_id(): int         │ ◄─── Getters públicos
                    │ + get_nome(): str       │
                    │ + get_email(): str      │
                    │ + set_nome(nome): void  │ ◄─── Setters públicos
                    │ + validar_senha(): bool │
                    │ {abstract} get_tipo()   │
                    └─────────────────────────┘
                              △
                              │
                 ┌────────────┴────────────┐
                 │      HERANÇA            │
                 │   Especialização        │
                 │                         │
        ┌────────┴─────────┐      ┌───────┴──────────┐
        │                  │      │                  │
┌───────────────────┐ ┌──────────────────────┐
│     Cliente       │ │   Administrador      │
├───────────────────┤ ├──────────────────────┤
│ - _cpf: str       │ │ - _cargo: str        │
│ - _telefone: str  │ │ - _nivel_acesso: int │
├───────────────────┤ ├──────────────────────┤
│ + get_cpf(): str  │ │ + get_cargo(): str   │
│ + get_tipo(): str │ │ + get_tipo(): str    │
│   return "cliente"│ │   return "admin"     │
│ + fazer_pedido()  │ │ + gerenciar_loja()   │
└───────────────────┘ └──────────────────────┘
         │ 1                        │
         │                          │
         │ tem                      │
         ▼ 0..*                     │
┌───────────────────┐               │
│    Endereco       │               │
├───────────────────┤               │
│ - _id: int        │               │
│ - _cliente_id: int│               │
│ - _cep: str       │               │
│ - _rua: str       │               │
│ - _numero: str    │               │
│ - _cidade: str    │               │
│ - _estado: str    │               │
│ - _principal: bool│               │
└───────────────────┘               │


┌─────────────────────────────────────────────────────────────────────┐
│              CAMADA DE CATÁLOGO - ASSOCIAÇÃO                        │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐              ┌─────────────────────────┐
│   Categoria      │ 1            │       Produto           │
├──────────────────┤─────────────►├─────────────────────────┤
│ - _id: int       │   pertence   │ - _id: int              │
│ - _nome: str     │      a       │ - _sku: str  (UNIQUE)   │
│ - _descricao: str│     0..*     │ - _nome: str            │
├──────────────────┤              │ - _descricao: str       │
│ + get_nome()     │              │ - _preco: Decimal       │
│ + listar_prods() │              │ - _estoque: int         │
└──────────────────┘              │ - _categoria_id: int    │
                                  │ - _ativo: bool          │
                                  ├─────────────────────────┤
                                  │ + get_preco(): Decimal  │
                                  │ + tem_estoque(): bool   │
                                  │ + abater_estoque(qtd)   │
                                  │ + validar_estoque()     │
                                  └─────────────────────────┘
                                             │ 1
                                             │
                                             │ possui
                                             │
                                             ▼ 0..5
                                  ┌─────────────────────────┐
                                  │   ImagemProduto         │
                                  ├─────────────────────────┤
                                  │ - _id: int              │
                                  │ - _produto_id: int      │
                                  │ - _url: str             │
                                  │ - _ordem: int           │
                                  │ - _principal: bool      │
                                  └─────────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│              CAMADA DE VENDAS - COMPOSIÇÃO                          │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│    Cliente       │
└──────────────────┘
         │ 1
         │ possui
         │
         ▼ 0..1
┌──────────────────────┐          ┌─────────────────────────┐
│     Carrinho         │ 1     0..*│   ItemCarrinho         │
├──────────────────────┤◆─────────►├─────────────────────────┤
│ - _id: int           │  contém   │ - _id: int              │
│ - _cliente_id: int   │           │ - _carrinho_id: int     │
│ - _criado_em: date   │           │ - _produto_id: int      │
├──────────────────────┤           │ - _quantidade: int      │
│ + adicionar_item()   │           │ - _preco_unitario: Dec  │
│ + remover_item()     │           ├─────────────────────────┤
│ + calcular_total()   │           │ + get_subtotal(): Dec   │
│ + limpar()           │           │ + alterar_qtd(qtd)      │
└──────────────────────┘           └─────────────────────────┘
                                              │ N
                                              │ referencia
                                              ▼ 1
                                   ┌─────────────────────┐
                                   │     Produto         │
                                   └─────────────────────┘

         ┌─────────┐
         │ Cliente │
         └─────────┘
              │ 1
              │ faz
              │
              ▼ 0..*
┌───────────────────────┐          ┌─────────────────────────┐
│       Pedido          │ 1     1..*│    ItemPedido          │
├───────────────────────┤◆─────────►├─────────────────────────┤
│ - _id: int            │  contém   │ - _id: int              │
│ - _cliente_id: int    │           │ - _pedido_id: int       │
│ - _status: StatusEnum │           │ - _produto_id: int      │
│ - _total: Decimal     │           │ - _quantidade: int      │
│ - _frete: Decimal     │           │ - _preco_unitario: Dec  │
│ - _endereco_entrega   │           │ - _subtotal: Decimal    │
│ - _forma_pagamento    │           ├─────────────────────────┤
│ - _data_pedido: date  │           │ + calcular_subtotal()   │
├───────────────────────┤           └─────────────────────────┘
│ + calcular_total()    │                      │ N
│ + alterar_status(s)   │                      │ referencia
│ + validar_estoque()   │                      ▼ 1
│ + processar()         │           ┌─────────────────────┐
└───────────────────────┘           │     Produto         │
         │                          └─────────────────────┘
         │ usa
         ▼
┌──────────────────┐
│  «enumeration»   │
│  StatusPedido    │
├──────────────────┤
│ PROCESSANDO      │
│ PAGAMENTO_CONF   │
│ SEPARANDO        │
│ ENVIADO          │
│ ENTREGUE         │
│ CANCELADO        │
└──────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│         CAMADA DE INTEGRAÇÕES - POLIMORFISMO                        │
└─────────────────────────────────────────────────────────────────────┘

                 ┌───────────────────────────────┐
                 │      «interface»              │
                 │    PagamentoGateway           │
                 ├───────────────────────────────┤
                 │ {abstract} processar(valor,   │
                 │            dados): bool       │
                 │ {abstract} validar(): bool    │
                 │ {abstract} cancelar(): bool   │
                 └───────────────────────────────┘
                              △
                              │
                 ┌────────────┴─────────────┐
                 │     POLIMORFISMO         │
                 │   Múltiplas implementa-  │
                 │   ções da mesma interface│
                 │                          │
        ┌────────┴──────────┐      ┌───────┴─────────┐
        │                   │      │                 │
┌───────────────────┐ ┌──────────────────────┐
│ PagamentoCartao   │ │   PagamentoPix       │
├───────────────────┤ ├──────────────────────┤
│ - _numero: str    │ │ - _chave_pix: str    │
│ - _cvv: str       │ │ - _qr_code: str      │
│ - _validade: str  │ │ - _expiracao: int    │
├───────────────────┤ ├──────────────────────┤
│ + processar()     │ │ + processar()        │
│   // Valida cartão│ │   // Gera QR code    │
│   // Cobra valor  │ │   // Aguarda confirm │
│ + validar()       │ │ + validar()          │
│ + cancelar()      │ │ + cancelar()         │
└───────────────────┘ └──────────────────────┘


                 ┌───────────────────────────────┐
                 │      «interface»              │
                 │   FreteCalculadora            │
                 ├───────────────────────────────┤
                 │ {abstract} calcular(cep_orig, │
                 │            cep_dest,          │
                 │            peso): Decimal     │
                 │ {abstract} prazo_entrega()    │
                 └───────────────────────────────┘
                              △
                              │
                              │ POLIMORFISMO
                              │
                 ┌────────────┴─────────────┐
                 │                          │
        ┌────────┴──────────┐      ┌───────┴─────────┐
        │                   │      │                 │
┌───────────────────┐ ┌──────────────────────┐
│ FreteCorreios     │ │   FreteExpresso      │
├───────────────────┤ ├──────────────────────┤
│ - _modalidade: str│ │ - _taxa_fixa: Dec    │
│ - _codigo_rastr.  │ ├──────────────────────┤
├───────────────────┤ │ + calcular()         │
│ + calcular()      │ │   // Taxa fixa       │
│   // API Correios │ │ + prazo_entrega()    │
│ + prazo_entrega() │ │   // Sempre 2 dias   │
└───────────────────┘ └──────────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│         CAMADA DE REPOSITÓRIOS - ABSTRAÇÃO                          │
└─────────────────────────────────────────────────────────────────────┘

                 ┌───────────────────────────────┐
                 │      «interface»              │
                 │    BaseRepository<T>          │
                 ├───────────────────────────────┤
                 │ {abstract} salvar(obj: T)     │
                 │ {abstract} buscar_por_id(id)  │
                 │ {abstract} listar(): List[T]  │
                 │ {abstract} atualizar(obj: T)  │
                 │ {abstract} deletar(id: int)   │
                 └───────────────────────────────┘
                              △
                              │
                 ┌────────────┴────────────┐
                 │                         │
        ┌────────┴──────────┐    ┌────────┴─────────┐
        │                   │    │                  │
┌──────────────────┐  ┌─────────────────┐  ┌──────────────────┐
│ProdutoRepository │  │ClienteRepository│  │ PedidoRepository │
├──────────────────┤  ├─────────────────┤  ├──────────────────┤
│ + salvar()       │  │ + salvar()      │  │ + salvar()       │
│ + buscar_por_sku │  │ + buscar_por_cpf│  │ + buscar_por_    │
│ + listar_por_cat │  │ + listar_ativos │  │   cliente()      │
│ + atualizar()    │  │ + atualizar()   │  │ + listar_por_    │
│ + deletar()      │  │ + deletar()     │  │   status()       │
└──────────────────┘  └─────────────────┘  └──────────────────┘


┌─────────────────────────────────────────────────────────────────────┐
│              CAMADA DE SERVIÇOS - ORQUESTRAÇÃO                      │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐
│   AuthService       │
├─────────────────────┤
│ - user_repository   │
│ - security_util     │
├─────────────────────┤
│ + registrar()       │
│ + autenticar()      │
│ + validar_token()   │
└─────────────────────┘

┌─────────────────────┐
│  CatalogoService    │
├─────────────────────┤
│ - produto_repo      │
│ - categoria_repo    │
├─────────────────────┤
│ + buscar_produtos() │
│ + filtrar()         │
│ + listar_por_cat()  │
└─────────────────────┘

┌─────────────────────┐         ┌──────────────────┐
│  CheckoutService    │────────►│ PagamentoGateway │
├─────────────────────┤  usa    ├──────────────────┤
│ - carrinho_service  │         │ + processar()    │
│ - pedido_repo       │         └──────────────────┘
│ - pagamento_gateway │
│ - frete_calculadora │         ┌──────────────────┐
├─────────────────────┤────────►│FreteCalculadora  │
│ + processar_compra()│  usa    ├──────────────────┤
│ + validar_estoque() │         │ + calcular()     │
│ + criar_pedido()    │         └──────────────────┘
│ + enviar_email()    │
└─────────────────────┘         ┌──────────────────┐
         │                      │  EmailService    │
         └─────────────────────►├──────────────────┤
                       usa      │ + enviar()       │
                                └──────────────────┘
```

---

## 📋 LEGENDA DOS RELACIONAMENTOS

### Símbolos UML:

**Herança (Generalização):**
```
     △
     │  (triângulo vazio apontando para classe pai)
```

**Composição (parte-todo forte):**
```
  ◆────►  (diamante preenchido - parte não existe sem o todo)
```
Exemplo: Pedido ◆──► ItemPedido (se deletar Pedido, deleta ItemPedido)

**Agregação (parte-todo fraco):**
```
  ◇────►  (diamante vazio)
```
Exemplo: Categoria ◇──► Produto (produto pode existir sem categoria)

**Associação:**
```
  ────►  (linha simples com seta)
```

**Multiplicidades:**
- `1` : exatamente um
- `0..1` : zero ou um (opcional)
- `0..*` : zero ou muitos
- `1..*` : um ou muitos

**Visibilidade:**
- `+` : público
- `-` : privado
- `#` : protegido

---

## 🎯 COMO USAR ESSAS ESPECIFICAÇÕES

### No Draw.io (Casos de Uso):
1. Acesse draw.io
2. Novo diagrama → UML → Use Case Diagram
3. Arraste os elementos:
   - **Actor** (boneco)
   - **Use Case** (elipse)
   - **System Boundary** (retângulo)
4. Conecte com linhas:
   - Linha simples: associação
   - Linha tracejada com «include»
   - Linha tracejada com «extend»

### No StarUML (Classes):
1. Novo Project → UML → Class Diagram
2. Adicione classes (retângulos)
3. Divida em 3 compartimentos:
   - Nome da classe
   - Atributos (- privado, + público)
   - Métodos
4. Conecte com:
   - **Generalization** (herança - triângulo)
   - **Composition** (diamante preenchido)
   - **Association** (linha simples)

---

## ✅ CHECKLIST - CONCEITOS POO NO DIAGRAMA

- [x] **Abstração**: BaseModel, BaseRepository, PagamentoGateway
- [x] **Encapsulamento**: Atributos privados (- _nome)
- [x] **Herança**: Usuario → Cliente, Administrador
- [x] **Polimorfismo**: PagamentoGateway com 2 implementações
- [x] **Composição**: Pedido ◆─→ ItemPedido
- [x] **Associação**: Cliente ──→ Pedido

---

## 💡 DICAS PARA A APRESENTAÇÃO

**Ao mostrar o Diagrama de Classes, destacar:**

1. **"Aqui está a ABSTRAÇÃO"**: Aponte BaseModel
2. **"Aqui está a HERANÇA"**: Mostre Usuario → Cliente
3. **"Aqui está o ENCAPSULAMENTO"**: Atributos privados (-)
4. **"Aqui está o POLIMORFISMO"**: PagamentoGateway com múltiplas implementações
5. **"Aqui está a COMPOSIÇÃO"**: Pedido contém ItemPedido

**Exemplo de fala:**
> "Aplicamos os 4 pilares da POO: criamos classes abstratas como BaseModel para ABSTRAÇÃO, usamos HERANÇA com Usuario sendo especializado em Cliente e Administrador, aplicamos ENCAPSULAMENTO com atributos privados e métodos públicos, e implementamos POLIMORFISMO com PagamentoGateway tendo múltiplas implementações como PagamentoCartao e PagamentoPix."

